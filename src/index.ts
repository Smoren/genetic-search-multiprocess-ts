import type {
  MultiprocessingMetricsStrategyConfig,
} from './types';

import {
  BaseMultiprocessingMetricsStrategy,
} from './strategies';

export type {
  MultiprocessingMetricsStrategyConfig,
};

export {
  BaseMultiprocessingMetricsStrategy,
};
